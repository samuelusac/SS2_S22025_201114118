-- Información del entrenamiento
SELECT *
FROM ML.TRAINING_INFO(MODEL `seminariosistemas2.nyc_taxi.fase2_tipped_logistic`);

SELECT *
FROM ML.TRAINING_INFO(MODEL `seminariosistemas2.nyc_taxi.fase2_tipped_btree`);